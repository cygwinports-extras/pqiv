--- origsrc/pqiv-0.7.1/Makefile	2008-05-19 10:18:27.000000000 -0500
+++ src/pqiv-0.7.1/Makefile	2008-05-26 08:27:35.921875000 -0500
@@ -13,9 +13,9 @@
 # pqiv
 all: pqiv manpage
 pqiv: 
-	$(CC) $(CFLAGS) `pkg-config --libs --cflags $(REQUIRED_PACKAGES)` -o qiv $(OPTIONFLAGS) pqiv.c
+	$(CC) $(CFLAGS) pqiv.c `pkg-config --libs --cflags $(REQUIRED_PACKAGES)` -o qiv $(OPTIONFLAGS)
 debug: 
-	$(CC) $(CGLAGS) -Wall -ggdb -DDEBUG `pkg-config --libs --cflags $(REQUIRED_PACKAGES)` $(OPTIONFLAGS) -o qiv pqiv.c
+	$(CC) $(CFLAGS) pqiv.c -Wall -ggdb -DDEBUG `pkg-config --libs --cflags $(REQUIRED_PACKAGES)` $(OPTIONFLAGS) -o qiv
 
 # The manpage stuff is kind of hackish, but it seems that I can't rely on the C
 # preprocessor (drac from gentoo reported "missing terminating ' character"
